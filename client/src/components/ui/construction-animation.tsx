import React from 'react';
import { motion } from 'framer-motion';

const ConstructionAnimation: React.FC = () => {
  return (
    <div className="relative w-40 h-24 my-8">
      {/* Esta animación ya no se usa pero mantenemos el componente
         para no romper otras posibles referencias */}
    </div>
  );
};

export default ConstructionAnimation;